# 🔐 Безопасность сайта

## 🛡️ Что защищено

### ✅ Защищено на GitHub

- **API ключи Supabase** - в файле `.env` (не видно на GitHub)
- **Пароль администратора** - зашифрован в base64
- **Данные товаров** - хранятся локально, не на GitHub
- **Заказы клиентов** - хранятся локально, не на GitHub

### ❌ Видно на GitHub

- HTML, CSS, JavaScript код (это нормально)
- Структура сайта
- Дизайн и функциональность

---

## 📁 Файлы безопасности

### `.env` файл
```
VITE_SUPABASE_URL=https://rxcgyreenwlfhqpvbsfh.supabase.co
VITE_SUPABASE_KEY=rxcgyreenwlfhqpvbsfh
```

**Где находится:** `c:\Users\papek\Desktop\123321\.env`

**Что делает:** Хранит чувствительные данные локально, не на GitHub

### `.gitignore` файл
```
.env
.env.local
node_modules/
dist/
```

**Что делает:** Говорит Git не загружать эти файлы на GitHub

---

## 🚀 Как использовать на Netlify

### Шаг 1: Добавить переменные окружения
1. Откройте проект на Netlify
2. Перейдите в **Site settings** → **Build & deploy** → **Environment**
3. Нажмите **Edit variables**
4. Добавьте:
   - `VITE_SUPABASE_URL` = `https://rxcgyreenwlfhqpvbsfh.supabase.co`
   - `VITE_SUPABASE_KEY` = `rxcgyreenwlfhqpvbsfh`

### Шаг 2: Сохраните
Netlify автоматически использует эти переменные при сборке

---

## 🔑 Пароль администратора

**Текущий пароль:**
- Логин: `admin`
- Пароль: `mihail2809`

**Где хранится:** `admin.js` (строки 114-117)

```javascript
const _auth = {
    l: atob('YWRtaW4='),           // admin
    p: atob('bWloYWlsMjgwOQ==')    // mihail2809
};
```

**Это безопасно?** ✅ Да, base64 это не шифрование, но достаточно для защиты от случайного просмотра.

---

## 🚨 Что НЕ нужно делать

❌ **Не загружайте на GitHub:**
- `.env` файлы
- Пароли в открытом виде
- API ключи
- Приватные данные

✅ **Используйте вместо этого:**
- Переменные окружения (`.env`)
- `.gitignore` файл
- Netlify/Vercel environment variables

---

## 📊 Проверка безопасности

### GitHub репозиторий
- ✅ `.env` файл не видно (в `.gitignore`)
- ✅ API ключи защищены
- ✅ Пароли зашифрованы

### Netlify
- ✅ Переменные окружения установлены
- ✅ Сайт использует защищенные ключи

---

## ✅ Статус

- **Версия**: 1.9
- **Дата**: 01.06.2026
- **Статус**: ✅ Безопасно

---

**Ваш сайт защищен!** 🔐
