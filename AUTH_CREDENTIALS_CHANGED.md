# 🔐 Изменение учетных данных админ-панели

## 🎯 Что было сделано

Изменены учетные данные для входа в админ-панель.

## 📊 Изменения

### Новые учетные данные

| Параметр | Значение |
|----------|----------|
| **Логин** | `admin` |
| **Пароль** | `mihail2809` |

### Старые учетные данные

| Параметр | Значение |
|----------|----------|
| Логин | admin |
| Пароль | admin123 |

## 🔧 Исправленные файлы

### 1. admin.js (строки 113-118)

**Было:**
```javascript
const _auth = {
    l: atob('YWRtaW4='),           // admin
    p: atob('YWRtaW4xMjM=')        // admin123
};
```

**Стало:**
```javascript
const _auth = {
    l: atob('YWRtaW4='),           // admin
    p: atob('bWloYWlsMjgwOQ==')    // mihail2809
};
```

### 2. admin.html (строка 68)

**Было:**
```html
<p class="login-hint">Логін: admin | Пароль: admin123</p>
```

**Стало:**
```html
<p class="login-hint">Логін: admin | Пароль: mihail2809</p>
```

## 🔐 Безопасность

- Пароль закодирован в base64 для скрытия от простого просмотра
- Base64 кодирование: `mihail2809` → `bWloYWlsMjgwOQ==`
- Это не криптографическое шифрование, а просто кодирование для скрытия

## 🚀 Как использовать

### Вход в админ-панель

1. Откройте `admin.html` в браузере
2. Введите логин: `admin`
3. Введите пароль: `mihail2809`
4. Нажмите "Увійти"

### Если забыли пароль

Пароль хранится в файле `admin.js` в переменной `_auth.p`. Вы можете:
1. Открыть `admin.js` в редакторе
2. Найти строку с `_auth`
3. Изменить значение `p` на новый пароль (в base64)

## 📝 Примечания

- Логин остался прежним: `admin`
- Пароль изменен с `admin123` на `mihail2809`
- Подсказка на странице входа обновлена

## ✅ Статус

- **Версия**: 1.5
- **Дата**: 01.06.2026
- **Статус**: ✅ Готово к использованию

---

**Готово!** Учетные данные админ-панели изменены.
